import { c as createComponent, r as renderTemplate, h as defineScriptVars, g as renderComponent, d as createAstro, m as maybeRenderHead } from '../chunks/astro/server_Cfy4trcV.mjs';
import { $ as $$Layout } from '../chunks/Layout_Dz_HxGPl.mjs';
export { renderers } from '../renderers.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Astro = createAstro();
const prerender = false;
const $$Login = createComponent(async ($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$Astro, $$props, $$slots);
  Astro2.self = $$Login;
  const callbackUrl = Astro2.url.searchParams.get("callbackUrl") ?? "/dashboard";
  const title = "Connexion | FlexPad";
  return renderTemplate(_a || (_a = __template(["", " <script>(function(){", "\n  const form = document.getElementById('login-form');\n  const errEl = document.getElementById('login-error');\n\n  /** Connexion credentials sans `auth-astro/client` : leur `signIn` fait `new URL(data.url)` sans base, ce qui jette si Auth renvoie une URL relative (succ\xE8s). */\n  async function signInCredentials(email, password, nextUrl) {\n    const prefix = '/api/auth';\n    const csrfRes = await fetch(`${prefix}/csrf`, { credentials: 'same-origin' });\n    if (!csrfRes.ok) throw new Error(`CSRF HTTP ${csrfRes.status}`);\n    const csrfJson = await csrfRes.json();\n    const csrfToken = csrfJson?.csrfToken;\n    if (!csrfToken) throw new Error('Jeton CSRF manquant');\n\n    const res = await fetch(`${prefix}/callback/credentials`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/x-www-form-urlencoded',\n        'X-Auth-Return-Redirect': '1',\n      },\n      credentials: 'same-origin',\n      body: new URLSearchParams({\n        email: String(email ?? ''),\n        password: String(password ?? ''),\n        csrfToken,\n        callbackUrl: nextUrl,\n      }),\n    });\n\n    let data;\n    try {\n      data = await res.json();\n    } catch (parseErr) {\n      console.error('[login] JSON invalide', parseErr);\n      throw new Error('R\xE9ponse serveur invalide');\n    }\n\n    const rawUrl = typeof data?.url === 'string' ? data.url : '';\n    let target = nextUrl;\n    let authError = null;\n    if (rawUrl) {\n      try {\n        const u =\n          rawUrl.startsWith('http://') || rawUrl.startsWith('https://')\n            ? new URL(rawUrl)\n            : new URL(rawUrl, window.location.origin);\n        authError = u.searchParams.get('error');\n        target = u.href;\n      } catch (urlErr) {\n        console.error('[login] URL de redirection invalide', rawUrl, urlErr);\n        throw new Error('Redirection invalide');\n      }\n    }\n\n    if (authError) {\n      return { ok: false, error: 'credentials' };\n    }\n    return { ok: true, redirectTo: target };\n  }\n\n  form?.addEventListener('submit', async (e) => {\n    e.preventDefault();\n    if (errEl) {\n      errEl.hidden = true;\n      errEl.textContent = '';\n    }\n    const fd = new FormData(form);\n    const email = fd.get('email');\n    const password = fd.get('password');\n    const nextUrl = callbackUrl || '/dashboard';\n    try {\n      const out = await signInCredentials(email, password, nextUrl);\n      if (!out.ok) {\n        if (errEl) {\n          errEl.textContent = 'Courriel ou mot de passe incorrect.';\n          errEl.hidden = false;\n        }\n        return;\n      }\n      window.location.assign(out.redirectTo);\n    } catch (err) {\n      console.error('[login]', err);\n      if (errEl) {\n        errEl.textContent = 'Une erreur est survenue. R\xE9essayez.';\n        errEl.hidden = false;\n      }\n    }\n  });\n})();<\/script>"], ["", " <script>(function(){", "\n  const form = document.getElementById('login-form');\n  const errEl = document.getElementById('login-error');\n\n  /** Connexion credentials sans \\`auth-astro/client\\` : leur \\`signIn\\` fait \\`new URL(data.url)\\` sans base, ce qui jette si Auth renvoie une URL relative (succ\xE8s). */\n  async function signInCredentials(email, password, nextUrl) {\n    const prefix = '/api/auth';\n    const csrfRes = await fetch(\\`\\${prefix}/csrf\\`, { credentials: 'same-origin' });\n    if (!csrfRes.ok) throw new Error(\\`CSRF HTTP \\${csrfRes.status}\\`);\n    const csrfJson = await csrfRes.json();\n    const csrfToken = csrfJson?.csrfToken;\n    if (!csrfToken) throw new Error('Jeton CSRF manquant');\n\n    const res = await fetch(\\`\\${prefix}/callback/credentials\\`, {\n      method: 'POST',\n      headers: {\n        'Content-Type': 'application/x-www-form-urlencoded',\n        'X-Auth-Return-Redirect': '1',\n      },\n      credentials: 'same-origin',\n      body: new URLSearchParams({\n        email: String(email ?? ''),\n        password: String(password ?? ''),\n        csrfToken,\n        callbackUrl: nextUrl,\n      }),\n    });\n\n    let data;\n    try {\n      data = await res.json();\n    } catch (parseErr) {\n      console.error('[login] JSON invalide', parseErr);\n      throw new Error('R\xE9ponse serveur invalide');\n    }\n\n    const rawUrl = typeof data?.url === 'string' ? data.url : '';\n    let target = nextUrl;\n    let authError = null;\n    if (rawUrl) {\n      try {\n        const u =\n          rawUrl.startsWith('http://') || rawUrl.startsWith('https://')\n            ? new URL(rawUrl)\n            : new URL(rawUrl, window.location.origin);\n        authError = u.searchParams.get('error');\n        target = u.href;\n      } catch (urlErr) {\n        console.error('[login] URL de redirection invalide', rawUrl, urlErr);\n        throw new Error('Redirection invalide');\n      }\n    }\n\n    if (authError) {\n      return { ok: false, error: 'credentials' };\n    }\n    return { ok: true, redirectTo: target };\n  }\n\n  form?.addEventListener('submit', async (e) => {\n    e.preventDefault();\n    if (errEl) {\n      errEl.hidden = true;\n      errEl.textContent = '';\n    }\n    const fd = new FormData(form);\n    const email = fd.get('email');\n    const password = fd.get('password');\n    const nextUrl = callbackUrl || '/dashboard';\n    try {\n      const out = await signInCredentials(email, password, nextUrl);\n      if (!out.ok) {\n        if (errEl) {\n          errEl.textContent = 'Courriel ou mot de passe incorrect.';\n          errEl.hidden = false;\n        }\n        return;\n      }\n      window.location.assign(out.redirectTo);\n    } catch (err) {\n      console.error('[login]', err);\n      if (errEl) {\n        errEl.textContent = 'Une erreur est survenue. R\xE9essayez.';\n        errEl.hidden = false;\n      }\n    }\n  });\n})();<\/script>"])), renderComponent($$result, "Layout", $$Layout, { "title": title, "site": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="page-content auth-page"> <main class="content-section auth-page__main"> <h1 class="page-title">Connexion</h1> <p class="page-lead">Accédez à votre tableau de bord FlexPad.</p> <form class="auth-form" id="login-form" novalidate> <div class="form-group"> <label for="login-email">Courriel</label> <input type="email" id="login-email" name="email" class="form-control-like" required autocomplete="email"> </div> <div class="form-group"> <label for="login-password">Mot de passe</label> <input type="password" id="login-password" name="password" class="form-control-like" required autocomplete="current-password"> </div> <p class="auth-form__error" id="login-error" hidden></p> <button type="submit" class="btn-primary">Se connecter</button> </form> <p class="auth-page__hint">
Compte démo : <code>demo@flexpad.local</code> / <code>flexpad-demo</code> (après <code>pnpm run db:seed</code>).
</p> <div class="back-link"> <a href="/">← Retour à l’accueil</a> </div> </main> </div> ` }), defineScriptVars({ callbackUrl }));
}, "C:/Users/Mathieu/OneDrive - Cegep Gerald-Godin/Cegep Gerald-Godin/Session_6/Projet_Finale/Numpad/Projet_Final/src/pages/login.astro", void 0);

const $$file = "C:/Users/Mathieu/OneDrive - Cegep Gerald-Godin/Cegep Gerald-Godin/Session_6/Projet_Finale/Numpad/Projet_Final/src/pages/login.astro";
const $$url = "/login";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Login,
  file: $$file,
  prerender,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
